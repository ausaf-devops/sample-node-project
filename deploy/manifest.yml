apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-Deployment
spec:
  replicas: 2
  selector:
   matchLabels:
     app: my-Deployment
template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        -name: node.js-app-container
        image:  ausaf009/node.js-app:latest
        imagePullPolicy: Always
        port:
        - containerPort: 3005
